{
 "base":"97f6a7051e24924f359580237bbc2c027276bac3",
 "message":"test(merchant): exercise native partial and stack sales over the live connection",
 "purpose":"Extend rather than replace the retained offline realm/native checks. Walk a normal account to a real village merchant using validated movement, open the sale page through its real button, type a quantity with native input, and activate both sale buttons through the production GameConnection. Require exact authoritative quantities and gold, then verify the sale after reconnect. Retain all old combat timeouts and assertions; do not change source rewards, saves, or production item capacity.",
 "edits":[
  {"path":"client/Tests/LiveMerchantChecks.cs","expected_blob":null,"source_blob":"40f3916fd358513347e671347be6aa848bba3dbf"},
  {"path":"client/Tests/LiveExperienceContract.cs","expected_blob":"7142973299c1bc3df948a02a7eabab2d20b61c56","replacements":[
   {"old":"            long checkpointXp = Progression.Total(Self);","new":"            var merchantSale = await LiveMerchantChecks.Run(this, game, Require);\n            long checkpointXp = Progression.Total(Self);"},
   {"old":"            Require(Self.Equipment.GetValueOrDefault(\"weapon\") == weapon, \"GUI-equipped item identity survives reconnect\");","new":"            Require(Self.Gold == merchantSale.Gold && !Self.Inventory.Any(x => x.Id == merchantSale.ItemId), \"Native merchant sale quantities and exact gold survive reconnect\");\n            Require(Self.Equipment.GetValueOrDefault(\"weapon\") == weapon, \"GUI-equipped item identity survives reconnect\");"}
  ]}
 ]
}
